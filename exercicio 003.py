import os
os.system('cls || clear')

class Aluno:
    #consumidor.
    def __int__(self, nome: str, idade: int, endereco: Endereco) -> Nome:
        # Atributos 
        self.nome = nome
        self.idade = idade
        self.endereco = endereco

        def exibir_dados(self) -> str:
            return f"Nome: {self.nome} \nIdade: {self.idade} \nEndereço 